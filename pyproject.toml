[project]
name = "ns-shift-planner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=2.3.0",
    "pytest>=8.4.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.0",
]
